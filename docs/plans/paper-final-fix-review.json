{
 "objective": "Final independent acceptance of paper harness fixes and operational changes", "decomposed": false,
 "tasks": [{
  "id": "paper-final-fix-review", "agentRole": "reviewer", "agentId": "aorch-reviewer", "kind": "review",
  "risk": "critical", "complexity": "critical", "write": false, "allowedProviders": ["anthropic"],
  "allowedScope": ["quantpilot/paper", "quantpilot/tests", "quantpilot/packages/core", "quantpilot/packages/db", "scripts/verify-paper.py"],
  "forbiddenScope": [".env*", ".claude", ".codex", ".agents"],
  "objective": "Read-only cross-provider acceptance. Prior Opus review CONFIRMED all core safety fixes and AI/lab gates, but BLOCKED on: RefreshingClient proxy masked the real KIS closed submitter; unrelated remote holdings skipped known protection; trial strategy omitted from report; candidate status stale. Now gateway.begin explicitly passes RefreshingClient.current_client() (actual KisPaperClient) to coordinator, and test_paper_production_wiring performs actual client request construction/parsing through fake HTTP, asserting exactly one order-cash POST/accepted. Reconcile now returns False for remote supersets/unmanaged working orders, protects locally matched holdings, raises only local quantity divergence. Report/performance strategy union includes trades, and status resets. test_paper_review_regressions exercises them. Also audit changes SINCE prior snapshot: collector.py background thread (own SQLite connection, shared read-only market client/budget); auth.py mutex for renewal; cli.py starts/stops collector while holding account and runtime locks; jobs.py atomically finalizes report outbox+job, separate reporter process; store.verify_cash reconstructs cash and initial capital, gross fill PnL separately; reporting gross/cost/trial rows and nonbinding simplification suggestion; process.py caps hostile stdout/stderr and time, lab.py uses it with unique-container cleanup; research generation retries alternate once. No other novel features. Code formatted with Black py311. Assess these deltas and integration; do not re-audit unrelated legacy modules or demand global loss stops/profitability gates. Real network/manual paper/Slack/AI/Docker tests are deliberately excluded. No edits, no subdelegation, no external calls. Return PASS or BLOCK with concrete failure scenario, exact code location and correction. All test data is fake; never read actual credentials.",
  "acceptanceCriteria": ["Cross-provider safety acceptance or precise remaining blocker", "No edits or external calls"],
  "verificationCommands": ["python scripts/verify-paper.py quantpilot/tests/unit/test_paper_production_wiring.py quantpilot/tests/unit/test_paper_review_regressions.py quantpilot/tests/unit/test_paper_collector.py quantpilot/tests/unit/test_paper_process_bounds.py quantpilot/tests/unit/test_paper_reporting_worker.py quantpilot/tests/unit/test_paper_research_integration.py quantpilot/tests/unit/test_paper_strategy_lab.py"]
 }]
}
