---
name: aorch-downshift
description: Assess a lower-cost route for an already justified bounded delegation when quality requirements and available providers are known.
---

# Optional route assessment

First decide whether delegation is useful under adaptive-orchestrate. Small clear work can stay with the current agent; this skill does not require decomposition or a worker.

For an existing bounded task, use `node "C:/Users/goyan/OneDrive/문서/adaptive orchestrator/src/cli.js" classify --objective "<task objective>"` as a route suggestion, or `route --task <task.json>` when acceptance criteria, risk, capabilities or an explicit quality floor matter. Check that the model and effort are supported by the execution surface. The router’s prior is an estimate, not a quality guarantee; use relevant verification and revise the route when evidence warrants it. Do not override a user-requested model or downgrade the main conversation.

<!-- aorch-generated: skill:aorch-downshift; mode=native; edit integrations/shared/definitions.json -->
