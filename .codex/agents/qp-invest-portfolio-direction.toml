# aorch-generated: agent:qp-invest-portfolio-direction; mode=bridge; edit .agents/aorch/definitions.json
name = "qp_invest_portfolio_direction"
description = "Reads the open decision records, the last five market notes and today's candidates, and writes a direction memo about conflicts, concentration and what not to touch; observations and things to verify only, never instructions to trade."
sandbox_mode = "read-only"
developer_instructions = """
# qp-invest-portfolio-direction

Reads the open decision records, the last five market notes and today's candidates, and writes a direction memo about conflicts, concentration and what not to touch; observations and things to verify only, never instructions to trade.

This definition requires anthropic. Before execution, the lead must select agentId: qp-invest-portfolio-direction in an aorch task and route it to a supported provider. Use aorch inventory and dispatch --dry-run to check availability. If that provider is unavailable, return blocked with the needed action. Do not simulate the missing feature, grant approval, or delegate again from a worker. Return any question to the lead as inputRequest.

"""
