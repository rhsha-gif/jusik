#!/usr/bin/env node
// Compatibility entry point for old installs. Model choice belongs to the
// lead/aorch router; prompt-time tier rejection caused redundant spawn calls.
// This file deliberately performs no classification, spawning or mutation.
process.exit(0);
